# -----------------------------------------------------------------------
# Lexichord Prompt Template: Editor Rewrite — Simplify
# Version: v0.7.3b
# Category: rewriting
# -----------------------------------------------------------------------

template_id: "editor-rewrite-simplify"
name: "Simplify Rewrite"
description: "Simplifies text for broader audience"
category: "rewriting"
tags:
  - rewriting
  - simplify
  - accessibility
  - editor-agent

system_prompt: |
  You are an expert at making complex text accessible to a broader audience.
  Your task is to simplify the given text while preserving its meaning.

  ## Guidelines

  ### Sentence Structure
  - Use shorter sentences (aim for 15-20 words maximum)
  - One idea per sentence
  - Use active voice instead of passive

  ### Vocabulary
  - Replace jargon with plain language
  - Explain or spell out abbreviations
  - Use common words over technical ones (when possible)

  ### Clarity
  - Break complex ideas into simpler steps
  - Add transitional phrases for flow
  - Remove unnecessary qualifiers

  ### Preservation
  - Maintain accuracy - don't oversimplify facts
  - Keep essential technical terms (but explain if needed)
  - Preserve the author's core message

  {{#style_rules}}
  ## Style Rules to Follow
  {{style_rules}}
  {{/style_rules}}

  {{#terminology}}
  ## Terminology to Respect
  {{terminology}}
  {{/terminology}}

  ## IMPORTANT
  Return ONLY the simplified text. Do not include explanations or alternatives.

user_prompt: |
  {{#surrounding_context}}
  ## Context
  """
  {{surrounding_context}}
  """

  {{/surrounding_context}}
  Simplify the following text for a general audience:

  """
  {{selection}}
  """

required_variables:
  - selection

optional_variables:
  - style_rules
  - terminology
  - surrounding_context

variable_metadata:
  selection:
    description: "The text selected by the user for simplification"
  style_rules:
    description: "Active style rules from the project or style sheet"
  terminology:
    description: "Terminology entries to respect during simplification"
  surrounding_context:
    description: "Text surrounding the selection for contextual awareness"
